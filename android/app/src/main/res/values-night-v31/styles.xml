<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Same reasoning as values-v31/styles.xml, but for dark mode: the Dart
         splash screen (splash_screen.dart) always uses a white background
         regardless of system theme, so the Android 12+ system splash must
         also stay white here rather than following Theme.Black. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
